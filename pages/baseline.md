# Current verification: five-diagram baseline

Claim: Section 5.1 states that exactly five diagrams generate the first-order finite-width correction to the NTK mean.

The executable source is `reproduction/claim2_verifier.py`; the independent checker is `reproduction/claim2_independent_checker.py`; and the required failing control is `reproduction/claim2_negative_control.py`. The fixed command and pinned environment are in `.openresearch/artifacts/claim2/environment.md`.

The source contract, assumptions, exact quantifier, hashes, and anchors are visible in `.openresearch/artifacts/claim2/claim_contract.json` and `.openresearch/artifacts/claim2/source_audit.md`.

Pre-run reviewer verdict: **BLOCKED**. This page will be superseded additively by a cumulative child after the Hugging Face run log is captured. Historical judged Space pages remain preserved separately.
