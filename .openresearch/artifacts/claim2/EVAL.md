# Baseline evaluation

Status before the first remote run: **BLOCKED**.

The code, contract, checker, and negative control are committed. The status becomes VERIFIED only if the fixed command completes on Hugging Face `cpu-upgrade`, both positive checks pass, and the mutation exits nonzero. The next cumulative child will preserve the raw baseline output from the OpenResearch log.
