# Limitations

The executable demonstration fully reconstructs the `F` recursion and audits coverage signatures for `D/F/A/B`; it does not independently regenerate every long appendix expression for `D/A/B`. The final page must preserve this scope instead of upgrading it to the paper's separate all-orders theorem.
